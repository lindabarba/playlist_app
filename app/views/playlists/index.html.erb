<h1>Playlists</h1>

<% @playlists.each do |playlist| %>
<tr>
  <td><%= playlist.playlist_title %></td>&nbsp;&nbsp;
  <td><%= playlist.created_at %></td>&nbsp;&nbsp;
  <td><%= playlist.updated_at %></td><br>
  <!-- add playlist tags here once relationships established -->
</tr>
<% end %>
